import React from 'react';
import Timer from "../task-list/task-card/timer/Timer";
import "./Header.scss"

const Header = () => {
  return (
    <div>
      <Timer/>
    </div>
  );
};

export default Header;
